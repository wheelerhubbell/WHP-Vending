[build]
  command = "npm test && npm run check:public"
  publish = "public"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  external_node_modules = ["@netlify/database", "mppx", "stripe"]

[build.environment]
  NODE_VERSION = "24"
